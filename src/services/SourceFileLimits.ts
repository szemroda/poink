/** Shared limit for flat ODT sources and packaged ODT content.xml. */
export const MAX_ODT_XML_BYTES = 20 * 1024 ** 2;

/** Initial local plain-text ingestion limit. */
export const MAX_TEXT_SOURCE_BYTES = 20 * 1024 ** 2;
